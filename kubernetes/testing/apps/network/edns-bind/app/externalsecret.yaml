apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name edns-bind-rfc2136
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: openbao-store
    kind: ClusterSecretStore
  target:
    name: *name
  dataFrom:
    - extract:
        key: *name
